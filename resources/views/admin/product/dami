
 <x-app-layout>
    <div class="px-4 sm:px-6 lg:px-8 py-8 w-full max-w-7xl mx-auto dark:text-white">
        <h1 class="text-2xl font-bold text-gray-800 dark:text-white mb-6">Add a new product</h1>

        <form action="{{ route('products.store') }}" method="POST" enctype="multipart/form-data" class="space-y-6">
            @csrf

            <div class="grid grid-cols-1 lg:grid-cols-3 gap-6">

                <!-- Left Side: Main Inputs -->
                <div class="space-y-6 lg:col-span-2">

                    <!-- Product Information -->
                    <div class="bg-white dark:bg-gray-800 border border-gray-200 dark:border-gray-700 rounded-lg p-6">
                        <h2 class="text-lg font-semibold mb-4">Product Information</h2>

                        <div class="grid grid-cols-1 md:grid-cols-2 gap-4">
                            <div>
                                <label class="block font-medium">Product Name</label>
                                <input type="text" name="name" class="mt-1 border rounded w-full p-2" placeholder="Product name">
                            </div>
                            <div>
                                <label class="block font-medium">SKU</label>
                                <input type="text" name="sku" class="mt-1 border rounded w-full p-2" placeholder="SKU">
                            </div>
                            <div>
                                <label class="block font-medium">Barcode</label>
                                <input type="text" name="barcode" class="mt-1 border rounded w-full p-2" placeholder="Barcode">
                            </div>
                            <div>
                                <label class="block font-medium">Manufacture Date</label>
                                <input type="date" name="manufactureDate" min="0" max="100" class="border rounded w-full p-2">
                            </div>
                        </div>

                        <div class="mt-4">
                            <label class="block font-medium">Description (optional)</label>
                            <textarea name="description" rows="4" class="mt-1 border rounded w-full p-2"></textarea>

                            {{-- <input type="text" name="description" min="0" max="100" class="border rounded w-full p-2"> --}}
                        </div>
                    </div>

                    <!-- Product Image -->
                    <div class="bg-white dark:bg-gray-800 border border-gray-200 dark:border-gray-700 rounded-lg p-6">
                        <h2 class="text-lg font-semibold mb-4">Product Image</h2>
                        <div class="border-2 border-dashed rounded-lg p-6 flex flex-col items-center justify-center text-gray-500">
                            <input type="file" name="images[]" multiple class="hidden" id="imageUpload">
                            <input type="file" name="images[]" multiple class="border rounded w-full p-2">
                            <label for="imageUpload" class="cursor-pointer flex flex-col items-center">
                                <svg class="w-10 h-10 mb-2" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                                    <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2"
                                          d="M4 16l4-4m0 0l4 4m-4-4v12M12 4h8m-4 4h4m-4 4h4m-4 4h4"/>
                                </svg>
                                <span>Drag and drop your image here</span>
                                <span class="text-blue-600">or Browse</span>
                            </label>
                        </div>
                    </div>

                    <!-- Variants -->
                    {{-- <div class="bg-white dark:bg-gray-800 border border-gray-200 dark:border-gray-700 rounded-lg p-6">
                        <h2 class="text-lg font-semibold mb-4">Variants</h2>
                        <div class="flex items-center space-x-4">
                            <select name="variant" class="border rounded p-2">
                                <option value="">Select Variant</option>
                                <option value="size">Size</option>
                                <option value="color">Color</option>
                            </select>
                            <input type="text" name="variant_value" placeholder="e.g. 38" class="border rounded p-2">
                            <button type="button" class="bg-blue-600 text-white px-4 py-2 rounded">+ Add Another Option</button>
                        </div>
                    </div> --}}

                      <!-- Variants Information -->
                    <div class="bg-white dark:bg-gray-800 border border-gray-200 dark:border-gray-700 rounded-lg p-6">
                        <h2 class="text-lg font-semibold mb-4">Variants</h2>

                        <div class="grid grid-cols-1 md:grid-cols-2 gap-4">
                            <div>
                                <label class="block font-medium">Color</label>
                                <input type="text" name="color" class="mt-1 border rounded w-full p-2" placeholder="Color">
                            </div>
                            <div>
                                <label class="block font-medium">Care Instructions</label>
                                <input type="text" name="careInstructions" min="0" max="100" class="mt-1 border rounded w-full p-2" placeholder="Care Instructions">
                            </div>
                            <div>
                                <label class="block font-medium">Disclaimer</label>
                                <input type="text" name="disclaimer" min="0" max="100" class="mt-1 border rounded w-full p-2" placeholder="Disclaimer">
                            </div>
                            <div>
                                <label class="block font-medium">Net Quantity</label>
                                <input type="text" name="netQuantity" min="0" max="100" class="mt-1 border rounded w-full p-2" placeholder="Net Quantity">
                            </div>
                        </div>

                        <!-- Sizes & Quantities -->
                <div class="md:col-span-2 mt-6">
                    <h3 class="font-semibold mb-2">Sizes & Quantities</h3>
                    <div class="grid grid-cols-2 md:grid-cols-3 gap-4">
                        @foreach($sizes as $size)
                            <div class="flex items-center space-x-2">
                                <label class="w-12">{{ $size->name }}</label>
                                <input type="number" name="sizes[{{ $size->id }}]" value="0" min="0"
                                       class="border rounded w-full p-2">
                            </div>
                        @endforeach
                    </div>
                </div>
                    </div>

                    <!-- Inventory -->
                    {{-- <div class="bg-white dark:bg-gray-800 border border-gray-200 dark:border-gray-700 rounded-lg p-6">
                        <h2 class="text-lg font-semibold mb-4">Inventory</h2>
                        <div class="flex items-center space-x-4">
                            <input type="number" name="stock" placeholder="Add to Stock" class="border rounded p-2">
                            <button type="button" class="bg-blue-600 text-white px-4 py-2 rounded">Confirm</button>
                        </div>
                        <p class="text-sm text-gray-500 mt-2">Product in stock now: 54 | In transit: 390 | Last restocked: 24th June, 2022 | Total stock: 2340</p>
                    </div> --}}

                </div>

                <!-- Right Side: Pricing & Organization -->
                <div class="space-y-6">

                    <!-- Pricing -->
                    <div class="bg-white dark:bg-gray-800 border border-gray-200 dark:border-gray-700 rounded-lg p-6">
                        <h2 class="text-lg font-semibold mb-4">Pricing</h2>
                        <div>
                            <label class="block font-medium">New Price</label>
                            <input type="number" name="price" step="0.01" class="mt-1 border rounded w-full p-2">
                        </div>
                         <div class="mt-4">
                            <label class="block font-medium">Old Price</label>
                            <input type="number" name="old_price" step="0.01" class="mt-1 border rounded w-full p-2">
                        </div>
                        <div class="mt-4">
                            <label class="block font-medium">Discounted Price (%)</label>
                            <input type="number" name="discount_percent" min="0" max="100" class="mt-1 border rounded w-full p-2">
                        </div>
                        <div class="mt-4 flex items-center space-x-2">
                            <input type="checkbox" name="is_popular" value="1" class="rounded">
                            <span>Popular Product</span>
                        </div>
                        <div class="mt-4 flex items-center space-x-2">
                            <input type="checkbox" name="is_new" value="1" class="rounded">
                            <span>New Arrival</span>
                        </div>
                        <div class="mt-4 flex items-center space-x-2">
                            <input type="checkbox" name="in_stock" class="rounded">
                            <span>In stock</span>
                        </div>
                        <div class="mt-4 flex items-center space-x-2">
                            <input type="checkbox" name="charge_tax" class="rounded">
                            <span>Charge Tax on this product</span>
                        </div>
                    </div>

                    <!-- Organize -->
                    <div class="bg-white dark:bg-gray-800 border border-gray-200 dark:border-gray-700 rounded-lg p-6">
                        <h2 class="text-lg font-semibold mb-4">Organize</h2>

                        <label class="block font-medium">Vendor</label>
                        <select name="vendor_id" class="border rounded w-full p-2 mb-3">
                            <option value="">Select Vendor</option>
                        </select>

                        <label class="block font-medium">Category</label>
                        <select name="category_id" id="category" class="border rounded w-full p-2 mb-3">
                            <option value="">Select Category</option>
                            @foreach($categories as $cat)
                                <option value="{{ $cat->id }}">{{ $cat->name }}</option>
                            @endforeach
                        </select>




                  <!-- SubCategory -->
                <div>
                    <label class="block font-medium">SubCategory</label>
                    <select name="sub_category_id" id="subcategory" class="border rounded w-full p-2">
                        <option value="">Select SubCategory</option>
                    </select>
                </div>


                        <!-- Collections (Multi-select) -->
                <div class="md:col-span-2">
                    <label class="block font-medium">Collections</label>
                    <select name="collections[]" multiple class="border rounded w-full p-2">
                        @foreach($collections as $col)
                            <option value="{{ $col->id }}">{{ $col->title }}</option>
                        @endforeach
                    </select>
                </div>

                        <label class="block font-medium">Status</label>
                        <select name="status" class="border rounded w-full p-2 mb-3">
                            <option value="">Select Status</option>
                            <option value="active">Active</option>
                            <option value="draft">Draft</option>
                        </select>

                        <label class="block font-medium">Tags</label>
                        <input type="text" name="tags" placeholder="Fashion, Trending, Summer" class="border rounded w-full p-2">
                    </div>



                </div>
            </div>

            <!-- Footer Buttons -->
            <div class="flex justify-end space-x-4">
                <button type="button" class="px-4 py-2 border rounded">Discard</button>
                <button type="button" class="px-4 py-2 bg-gray-200 rounded">Save Draft</button>
                <button type="submit" class="px-6 py-2 bg-blue-600 text-white rounded">Publish Product</button>
            </div>


        </form>
    </div>

    <!-- Subcategory script -->
   <script>
        let categories = @json($categories);

        document.getElementById('category').addEventListener('change', function () {
            let catId = this.value;
            let subSelect = document.getElementById('subcategory');
            subSelect.innerHTML = '<option value="">Select SubCategory</option>';

            let selectedCat = categories.find(c => c.id == catId);
            if (selectedCat && selectedCat.sub_categories) {
                selectedCat.sub_categories.forEach(sc => {
                    subSelect.innerHTML += `<option value="${sc.id}">${sc.name}</option>`;
                });
            }
        });
    </script>



</x-app-layout>
