export interface StateType {
  name: string;
  city: string[];
}

export interface StoreType {
  name: string;
  state: string;
  city: string;
  address: string;
  email: string;
  phone: string;
  store_hours: string;
}
